# Keep rules for the release build. Compose and AndroidX ship their own
# consumer rules, so this file stays empty until the app needs reflection-based
# libraries (e.g. supabase-kt / kotlinx.serialization in Phase 9).
